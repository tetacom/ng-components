import { FilterComponentBase } from './filter-component-base';

describe('FilterComponentBase', () => {
  it('should create an instance', () => {
    expect(new FilterComponentBase()).toBeTruthy();
  });
});
