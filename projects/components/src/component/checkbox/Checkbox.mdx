import * as stories from './Checkbox.stories.ts';
import { Story } from '@storybook/addon-docs/blocks';

# Basic Checkbox

# Button

I can embed a story (but not define one, since this file should not contain a `Meta`):

<Story id="example-checkbox--binary" />

<Canvas>
  <Story id="example-checkbox--list"></Story>
</Canvas>

```javascript
let test: string = 'test';
```
