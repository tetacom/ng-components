import { SelectValueDirective } from './select-value.directive';

describe('SelectValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectValueDirective();
    expect(directive).toBeTruthy();
  });
});
