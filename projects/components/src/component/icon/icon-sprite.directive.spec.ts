import { IconSpriteDirective } from './icon-sprite.directive';

describe('IconSpriteDirective', () => {
  it('should create an instance', () => {
    const directive = new IconSpriteDirective();
    expect(directive).toBeTruthy();
  });
});
