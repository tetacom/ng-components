import { DropdownContentDirective } from './dropdown-content.directive';

describe('DropdownContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownContentDirective();
    expect(directive).toBeTruthy();
  });
});
