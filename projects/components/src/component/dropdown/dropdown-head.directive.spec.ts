import { DropdownHeadDirective } from './dropdown-head.directive';

describe('DropdownHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownHeadDirective();
    expect(directive).toBeTruthy();
  });
});
