import { ExpandPanelHeadDirective } from './expand-panel-head.directive';

describe('ExpandPanelHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandPanelHeadDirective();
    expect(directive).toBeTruthy();
  });
});
