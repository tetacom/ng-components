import { FullScreenToggleDirective } from './full-screen-toggle.directive';

describe('FullScreenToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new FullScreenToggleDirective();
    expect(directive).toBeTruthy();
  });
});
