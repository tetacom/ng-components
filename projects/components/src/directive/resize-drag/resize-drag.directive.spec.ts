import { ResizeDragDirective } from './resize-drag.directive';

describe('ResizeGrabDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeDragDirective();
    expect(directive).toBeTruthy();
  });
});
