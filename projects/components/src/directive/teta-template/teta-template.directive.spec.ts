import { TetaTemplateDirective } from './teta-template.directive';

describe('TetaTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TetaTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
