import { AutoPositionDirective } from './auto-position.directive';

describe('AutoPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoPositionDirective();
    expect(directive).toBeTruthy();
  });
});
