import { DragSortItemDirective } from './drag-sort-item.directive';

describe('DragSortItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DragSortItemDirective();
    expect(directive).toBeTruthy();
  });
});
