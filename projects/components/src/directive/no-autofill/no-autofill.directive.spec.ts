import { NoAutofillDirective } from './no-autofill.directive';

describe('NoAutofillDirective', () => {
  it('should create an instance', () => {
    const directive = new NoAutofillDirective();
    expect(directive).toBeTruthy();
  });
});
