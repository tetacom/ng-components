import { DragPlaceholderDirective } from './drag-placeholder.directive';

describe('DragPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new DragPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
