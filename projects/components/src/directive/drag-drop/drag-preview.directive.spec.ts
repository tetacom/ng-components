import { DragPreviewDirective } from './drag-preview.directive';

describe('DragPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new DragPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
