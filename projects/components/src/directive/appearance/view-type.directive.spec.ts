import { ViewTypeDirective } from './view-type.directive';

describe('ViewTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewTypeDirective();
    expect(directive).toBeTruthy();
  });
});
