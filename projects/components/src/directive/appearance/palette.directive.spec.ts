import { PaletteDirective } from './palette.directive';

describe('PaletteDirective', () => {
  it('should create an instance', () => {
    const directive = new PaletteDirective();
    expect(directive).toBeTruthy();
  });
});
