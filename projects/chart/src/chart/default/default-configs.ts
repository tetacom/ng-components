import { IChartConfig } from '../model/i-chart-config';

export const defaultConfigs: IChartConfig = {
  series: [],
  xAxis: [
    {
      visible: true,
    },
  ],
  yAxis: [
    {
      visible: true,
    },
  ],
  gridLines: true,
};
