import { DraggablePointDirective } from './draggable-point.directive';

describe('DraggablePointDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggablePointDirective();
    expect(directive).toBeTruthy();
  });
});
